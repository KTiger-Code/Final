# เอกสารวิเคราะห์และออกแบบระบบ
# Student Course Management System

## 📑 สารบัญ
1. [วิเคราะห์ความต้องการผู้ใช้](#1-วิเคราะห์ความต้องการผู้ใช้-user-requirements)
2. [การออกแบบฐานข้อมูล](#2-การออกแบบฐานข้อมูล-database-design)
3. [การออกแบบ API](#3-การออกแบบ-api)
4. [การทดสอบ API](#4-การทดสอบ-api-postman)
5. [การจัดการเวอร์ชัน](#5-การจัดการเวอร์ชัน-github)
6. [Deployment](#6-deployment-render)

---

## 1. วิเคราะห์ความต้องการผู้ใช้ (User Requirements)

### 1.1 ผู้ใช้งานหลัก
- **อาจารย์** - จัดการคอร์สเรียน ดูรายชื่อนักเรียนที่ลงทะเบียน
- **นักเรียน** - ลงทะเบียนเรียน ดูคอร์สที่ลงทะเบียนไว้
- **เจ้าหน้าที่** - จัดการข้อมูลนักเรียน คอร์ส และการลงทะเบียนทั้งหมด

### 1.2 ความสามารถของระบบ
1. **จัดการนักเรียน** (Students Management)
   - เพิ่มนักเรียนใหม่
   - แก้ไขข้อมูลนักเรียน
   - ลบนักเรียน
   - ค้นหาและดูข้อมูลนักเรียน
   - ดูคอร์สที่นักเรียนลงทะเบียน

2. **จัดการคอร์สเรียน** (Courses Management)
   - เพิ่มคอร์สใหม่
   - แก้ไขข้อมูลคอร์ส
   - ลบคอร์ส
   - ค้นหาและดูข้อมูลคอร์ส
   - ดูรายชื่อนักเรียนที่ลงทะเบียนในคอร์ส

3. **ลงทะเบียนเรียน** (Enrollment)
   - ลงทะเบียนนักเรียนเข้าคอร์ส
   - ยกเลิกการลงทะเบียน
   - ดูประวัติการลงทะเบียน

4. **ดูรายงาน** (Reporting)
   - รายงานการลงทะเบียนทั้งหมด
   - รายงานนักเรียนในแต่ละคอร์ส
   - รายงานคอร์สของนักเรียน

### 1.3 Entity Relationship Diagram (ERD)

```
┌─────────────────────┐
│     STUDENTS        │
├─────────────────────┤
│ PK: id              │
│     fullname        │
│     email (UNIQUE)  │
│     major           │
│     created_at      │
└──────────┬──────────┘
           │
           │ 1
           │
           │
           │ M
┌──────────┴──────────┐
│   ENROLLMENTS       │
├─────────────────────┤
│ PK: id              │
│ FK: student_id      │
│ FK: course_id       │
│     enrollment_date │
└──────────┬──────────┘
           │ M
           │
           │
           │ 1
┌──────────┴──────────┐
│      COURSES        │
├─────────────────────┤
│ PK: id              │
│     name            │
│     description     │
│     credit          │
│     created_at      │
└─────────────────────┘

ความสัมพันธ์:
- 1 นักเรียนสามารถลงทะเบียนได้หลายคอร์ส (One-to-Many)
- 1 คอร์สสามารถมีนักเรียนหลายคนลงทะเบียน (One-to-Many)
- ความสัมพันธ์ระหว่าง Students และ Courses เป็นแบบ Many-to-Many
  ผ่านตาราง Enrollments
```

### 1.4 Use Case Diagram

**Actor: นักเรียน (Student)**
- ลงทะเบียนเรียนคอร์ส
- ดูคอร์สที่ลงทะเบียนไว้
- ยกเลิกการลงทะเบียน

**Actor: เจ้าหน้าที่ (Admin)**
- จัดการข้อมูลนักเรียน (CRUD)
- จัดการข้อมูลคอร์ส (CRUD)
- จัดการการลงทะเบียน (CRUD)
- ดูรายงานการลงทะเบียนทั้งหมด

**Actor: อาจารย์ (Instructor)**
- ดูรายชื่อนักเรียนในคอร์ส
- จัดการคอร์สที่สอน

```
Use Cases:
┌────────────────────────────────────┐
│  Student Course Management System  │
├────────────────────────────────────┤
│                                    │
│  [จัดการนักเรียน]                  │
│     - เพิ่มนักเรียน                 │
│     - แก้ไขข้อมูล                  │
│     - ลบนักเรียน                   │
│     - ค้นหานักเรียน                │
│                                    │
│  [จัดการคอร์ส]                     │
│     - เพิ่มคอร์ส                    │
│     - แก้ไขคอร์ส                   │
│     - ลบคอร์ส                      │
│     - ค้นหาคอร์ส                   │
│                                    │
│  [ลงทะเบียนเรียน]                  │
│     - ลงทะเบียนเข้าคอร์ส           │
│     - ยกเลิกการลงทะเบียน          │
│     - ดูคอร์สที่ลงทะเบียน          │
│                                    │
│  [ดูรายงาน]                        │
│     - รายงานการลงทะเบียน          │
│     - นักเรียนในคอร์ส              │
│     - คอร์สของนักเรียน             │
└────────────────────────────────────┘
```

---

## 2. การออกแบบฐานข้อมูล (Database Design - Supabase)

### 2.1 ตารางและความสัมพันธ์

#### ตาราง: **students**
```sql
CREATE TABLE students (
  id BIGSERIAL PRIMARY KEY,
  fullname VARCHAR(255) NOT NULL,
  email VARCHAR(255) UNIQUE NOT NULL,
  major VARCHAR(100) NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc', NOW())
);
```

**คำอธิบาย:**
- `id` - Primary Key (Auto-increment)
- `fullname` - ชื่อ-นามสกุลนักเรียน
- `email` - อีเมล (ต้องไม่ซ้ำกัน)
- `major` - สาขาวิชา
- `created_at` - วันที่สร้างข้อมูล

#### ตาราง: **courses**
```sql
CREATE TABLE courses (
  id BIGSERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  credit INTEGER NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc', NOW())
);
```

**คำอธิบาย:**
- `id` - Primary Key (Auto-increment)
- `name` - ชื่อคอร์ส
- `description` - รายละเอียดคอร์ส
- `credit` - หน่วยกิต
- `created_at` - วันที่สร้างข้อมูล

#### ตาราง: **enrollments**
```sql
CREATE TABLE enrollments (
  id BIGSERIAL PRIMARY KEY,
  student_id BIGINT NOT NULL REFERENCES students(id) ON DELETE CASCADE,
  course_id BIGINT NOT NULL REFERENCES courses(id) ON DELETE CASCADE,
  enrollment_date TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc', NOW()),
  UNIQUE(student_id, course_id)
);
```

**คำอธิบาย:**
- `id` - Primary Key (Auto-increment)
- `student_id` - Foreign Key อ้างอิงไปยัง students.id
- `course_id` - Foreign Key อ้างอิงไปยัง courses.id
- `enrollment_date` - วันที่ลงทะเบียน
- `UNIQUE(student_id, course_id)` - ป้องกันการลงทะเบียนซ้ำ

### 2.2 Primary Key และ Foreign Key

#### **Primary Key (PK)**
Primary Key คือ คอลัมน์ที่ใช้ระบุแต่ละแถวในตารางให้มีค่าไม่ซ้ำกัน

**คุณสมบัติ:**
- ต้องมีค่า NOT NULL
- ต้องมีค่าไม่ซ้ำกัน (UNIQUE)
- แต่ละตารางมีได้เพียง 1 Primary Key
- ใช้ในการอ้างอิงความสัมพันธ์ระหว่างตาราง

**ในระบบนี้:**
- `students.id` - Primary Key ของตาราง students
- `courses.id` - Primary Key ของตาราง courses
- `enrollments.id` - Primary Key ของตาราง enrollments

#### **Foreign Key (FK)**
Foreign Key คือ คอลัมน์ที่ใช้อ้างอิงไปยัง Primary Key ของตารางอื่น เพื่อสร้างความสัมพันธ์

**คุณสมบัติ:**
- อ้างอิงค่าจาก Primary Key ของตารางอื่น
- ช่วยรักษา Referential Integrity (ความถูกต้องของการอ้างอิง)
- สามารถตั้งค่า ON DELETE CASCADE เพื่อลบข้อมูลที่เกี่ยวข้องอัตโนมัติ

**ในระบบนี้:**
- `enrollments.student_id` → อ้างอิงไปยัง `students.id`
- `enrollments.course_id` → อ้างอิงไปยัง `courses.id`

**ตัวอย่างการทำงาน:**
```sql
-- ถ้าลบนักเรียน ID=1 จะลบการลงทะเบียนของนักเรียนคนนั้นทั้งหมดด้วย
DELETE FROM students WHERE id = 1;
-- enrollments ที่มี student_id = 1 จะถูกลบอัตโนมัติ (CASCADE)
```

### 2.3 Normalization (การทำให้ข้อมูลเป็นมาตรฐาน)

**Normalization** คือ กระบวนการออกแบบฐานข้อมูลเพื่อลด Data Redundancy (ข้อมูลซ้ำซ้อน) และป้องกัน Anomaly (ความผิดปกติ)

#### **1st Normal Form (1NF)**
✅ ทุกคอลัมน์มีค่าเป็น Atomic (ไม่แยกย่อยได้อีก)
- ✅ fullname เก็บเป็น string เดียว
- ✅ ไม่มีคอลัมน์ที่เก็บหลายค่า (เช่น course1, course2, course3)

#### **2nd Normal Form (2NF)**
✅ อยู่ใน 1NF และไม่มี Partial Dependency
- ✅ ทุกคอลัมน์ที่ไม่ใช่ PK ขึ้นอยู่กับ PK ทั้งหมด
- ✅ ตาราง enrollments แยก student และ course ออกจากกัน

#### **3rd Normal Form (3NF)**
✅ อยู่ใน 2NF และไม่มี Transitive Dependency
- ✅ ไม่มีคอลัมน์ที่ขึ้นอยู่กับคอลัมน์อื่นที่ไม่ใช่ PK
- ✅ ข้อมูลนักเรียนและคอร์สแยกตารางอย่างชัดเจน

#### **ประโยชน์ของ Normalization:**
1. **ลดข้อมูลซ้ำซ้อน** - ข้อมูลนักเรียนและคอร์สเก็บครั้งเดียว
2. **ง่ายต่อการแก้ไข** - แก้ชื่อนักเรียนที่เดียว ไม่ต้องแก้หลายที่
3. **รักษาความถูกต้อง** - Foreign Key ช่วยรักษา Integrity
4. **ประหยัดพื้นที่** - ไม่เก็บข้อมูลซ้ำ

#### **ตัวอย่างการหลีกเลี่ยงข้อมูลซ้ำ:**

**❌ แบบไม่ Normalize (ข้อมูลซ้ำ):**
```
enrollment_records:
| id | student_name | student_email | course_name | credit |
|----|--------------|---------------|-------------|--------|
| 1  | สมชาย        | som@mail.com  | Database    | 3      |
| 2  | สมชาย        | som@mail.com  | Web Dev     | 3      |
| 3  | สมหญิง       | som2@mail.com | Database    | 3      |
```
ปัญหา: ถ้าเปลี่ยนชื่อ "สมชาย" ต้องแก้หลายแถว, ข้อมูล Database ซ้ำ

**✅ แบบ Normalize (แยกตาราง):**
```
students:          courses:           enrollments:
| id | name   |    | id | name    |   | id | student_id | course_id |
|----|--------|    |----|---------|   |----|------------|-----------|
| 1  | สมชาย  |    | 1  | Database|   | 1  | 1          | 1         |
| 2  | สมหญิง |    | 2  | Web Dev |   | 2  | 1          | 2         |
                                       | 3  | 2          | 1         |
```
ข้อดี: แก้ข้อมูลที่เดียว, ไม่มีข้อมูลซ้ำ, ประหยัดพื้นที่

---

## 3. การออกแบบ API (Backend - Node.js + Express)

### 3.1 RESTful API Principles

**REST (Representational State Transfer)** คือ สถาปัตยกรรมการออกแบบ API ที่ใช้ HTTP Methods

#### **HTTP Methods ที่ใช้:**
- **GET** - ดึงข้อมูล (Read)
- **POST** - สร้างข้อมูลใหม่ (Create)
- **PUT** - แก้ไขข้อมูล (Update)
- **DELETE** - ลบข้อมูล (Delete)

#### **REST Principles:**
1. **Stateless** - ทุก request ต้องมีข้อมูลครบถ้วน ไม่พึ่งพา session
2. **Resource-based URLs** - ใช้ noun (คำนาม) ในการตั้งชื่อ endpoint
3. **HTTP Methods** - ใช้ method ที่เหมาะสมกับการทำงาน
4. **Status Codes** - ใช้ HTTP status codes ที่ถูกต้อง

#### **API Endpoints ที่ออกแบบตาม REST:**

**Students:**
```
GET    /api/students           → ดึงนักเรียนทั้งหมด
GET    /api/students/:id       → ดึงนักเรียนตาม ID
POST   /api/students           → สร้างนักเรียนใหม่
PUT    /api/students/:id       → แก้ไขนักเรียน
DELETE /api/students/:id       → ลบนักเรียน
GET    /api/students/:id/courses → ดูคอร์สของนักเรียน
```

**Courses:**
```
GET    /api/courses            → ดึงคอร์สทั้งหมด
GET    /api/courses/:id        → ดึงคอร์สตาม ID
POST   /api/courses            → สร้างคอร์สใหม่
PUT    /api/courses/:id        → แก้ไขคอร์ส
DELETE /api/courses/:id        → ลบคอร์ส
GET    /api/courses/:id/students → ดูนักเรียนในคอร์ส
```

**Enrollments:**
```
GET    /api/enrollments        → ดึงการลงทะเบียนทั้งหมด
GET    /api/enrollments/:id    → ดึงการลงทะเบียนตาม ID
POST   /api/enrollments        → ลงทะเบียนใหม่
DELETE /api/enrollments/:id    → ยกเลิกการลงทะเบียน
```

### 3.2 การจัดการ Router

**Router** ช่วยแยกการจัดการ endpoints ออกเป็นไฟล์ต่างๆ เพื่อให้โค้ดอ่านง่ายและจัดการง่าย

**โครงสร้าง Router:**
```
server.js (Main file)
   ├── /api/students    → routes/students.js
   ├── /api/courses     → routes/courses.js
   └── /api/enrollments → routes/enrollments.js
```

**ตัวอย่างการใช้งาน:**
```javascript
// server.js
const studentRoutes = require('./routes/students');
app.use('/api/students', studentRoutes);

// routes/students.js
const express = require('express');
const router = express.Router();

router.get('/', async (req, res) => {
  // Handle GET /api/students
});

router.post('/', async (req, res) => {
  // Handle POST /api/students
});

module.exports = router;
```

**ข้อดีของการใช้ Router:**
- แยก logic ตาม resource ชัดเจน
- ง่ายต่อการ maintain และเพิ่มฟีเจอร์
- โค้ดอ่านง่าย ไม่ยุ่งเหยิง

### 3.3 การเชื่อมต่อกับ Supabase

**Supabase** เป็น Backend as a Service (BaaS) ที่ให้บริการ PostgreSQL Database

**ขั้นตอนการเชื่อมต่อ:**

1. **ติดตั้ง Supabase Client:**
```bash
npm install @supabase/supabase-js
```

2. **สร้าง Configuration File:**
```javascript
// config/supabase.js
const { createClient } = require('@supabase/supabase-js');

const supabaseUrl = process.env.SUPABASE_URL;
const supabaseKey = process.env.SUPABASE_KEY;

const supabase = createClient(supabaseUrl, supabaseKey);

module.exports = supabase;
```

3. **ใช้งานใน Routes:**
```javascript
const supabase = require('../config/supabase');

// ดึงข้อมูล
const { data, error } = await supabase
  .from('students')
  .select('*');

// เพิ่มข้อมูล
const { data, error } = await supabase
  .from('students')
  .insert([{ fullname, email, major }]);

// แก้ไขข้อมูล
const { data, error } = await supabase
  .from('students')
  .update({ fullname })
  .eq('id', id);

// ลบข้อมูล
const { data, error } = await supabase
  .from('students')
  .delete()
  .eq('id', id);

// Join tables
const { data, error } = await supabase
  .from('enrollments')
  .select(`
    *,
    students (*),
    courses (*)
  `);
```

**คุณสมบัติของ Supabase:**
- 🔐 Authentication built-in
- 📊 Real-time subscriptions
- 🗄️ PostgreSQL database
- 🔒 Row Level Security (RLS)
- 📦 Storage for files
- 🚀 Auto-generated APIs

---

## 4. การทดสอบ API (Postman)

### 4.1 Test Cases สำหรับทุก API

#### **Students API**

**1. GET All Students**
```
Method: GET
URL: {{base_url}}/api/students
Expected: 200 OK
Response: Array of students
```

**2. GET Student by ID**
```
Method: GET
URL: {{base_url}}/api/students/1
Expected: 200 OK
Response: Single student object
```

**3. POST Create Student**
```
Method: POST
URL: {{base_url}}/api/students
Body:
{
  "fullname": "สมชาย ใจดี",
  "email": "somchai@example.com",
  "major": "Computer Science"
}
Expected: 201 Created
```

**4. PUT Update Student**
```
Method: PUT
URL: {{base_url}}/api/students/1
Body:
{
  "fullname": "สมชาย แก้ไข"
}
Expected: 200 OK
```

**5. DELETE Student**
```
Method: DELETE
URL: {{base_url}}/api/students/1
Expected: 200 OK
```

#### **Courses API**

**1. GET All Courses**
```
Method: GET
URL: {{base_url}}/api/courses
Expected: 200 OK
```

**2. POST Create Course**
```
Method: POST
URL: {{base_url}}/api/courses
Body:
{
  "name": "Database Systems",
  "description": "Learn SQL",
  "credit": 3
}
Expected: 201 Created
```

#### **Enrollments API**

**1. POST Create Enrollment**
```
Method: POST
URL: {{base_url}}/api/enrollments
Body:
{
  "student_id": 1,
  "course_id": 1
}
Expected: 201 Created
```

**2. GET Student's Courses**
```
Method: GET
URL: {{base_url}}/api/students/1/courses
Expected: 200 OK
Response: Array of enrolled courses
```

### 4.2 Expected Output และ Error Handling

#### **Success Response Format:**
```json
{
  "success": true,
  "message": "Operation completed successfully",
  "data": { ... },
  "count": 10
}
```

#### **Error Response Format:**
```json
{
  "success": false,
  "error": "Error message description"
}
```

#### **HTTP Status Codes:**
- `200 OK` - Success (GET, PUT, DELETE)
- `201 Created` - Resource created (POST)
- `400 Bad Request` - Invalid input
- `404 Not Found` - Resource not found
- `500 Internal Server Error` - Server error

---

## 5. การจัดการเวอร์ชัน (GitHub)

### 5.1 Git Workflow

**Git Workflow** คือ แนวทางการใช้งาน Git เพื่อจัดการ source code

#### **Feature Branch Workflow:**

```
main (production)
 │
 ├── develop (development)
     │
     ├── feature/student-api
     ├── feature/course-api
     ├── feature/enrollment-api
     └── bugfix/error-handling
```

#### **ขั้นตอนการทำงาน:**

**1. Clone Repository**
```bash
git clone <repository-url>
cd Final
```

**2. สร้าง Branch ใหม่สำหรับฟีเจอร์**
```bash
git checkout -b feature/student-api
```

**3. ทำงานและ Commit**
```bash
git add .
git commit -m "Add student CRUD endpoints"
```

**4. Push Branch ขึ้น GitHub**
```bash
git push origin feature/student-api
```

**5. สร้าง Pull Request**
- ไปที่ GitHub Repository
- คลิก "Pull Request"
- เลือก base: main ← compare: feature/student-api
- เขียน description อธิบายการเปลี่ยนแปลง
- Request review (ถ้ามีทีม)

**6. Merge เข้า Main**
```bash
# หลังจาก review และ approve
git checkout main
git merge feature/student-api
git push origin main
```

**7. ลบ Branch ที่ไม่ใช้แล้ว**
```bash
git branch -d feature/student-api
git push origin --delete feature/student-api
```

### 5.2 Best Practices

#### **Commit Messages:**
```bash
# ดี
git commit -m "Add student CRUD endpoints"
git commit -m "Fix enrollment duplicate validation"
git commit -m "Update README with API documentation"

# ไม่ดี
git commit -m "update"
git commit -m "fix bug"
git commit -m "changes"
```

#### **Branch Naming:**
```bash
feature/feature-name    # ฟีเจอร์ใหม่
bugfix/bug-description  # แก้ไขบั๊ก
hotfix/critical-fix     # แก้ไขเร่งด่วน
```

#### **.gitignore:**
```
node_modules/
.env
logs/
*.log
.DS_Store
```

---

## 6. Deployment (Render)

### 6.1 ขั้นตอน Deployment

#### **1. เตรียม GitHub Repository**
```bash
git add .
git commit -m "Prepare for deployment"
git push origin main
```

#### **2. สร้างบัญชี Render**
- ไปที่ [https://render.com](https://render.com)
- Sign up with GitHub

#### **3. สร้าง Web Service**
1. คลิก "New +" → "Web Service"
2. เชื่อมต่อ GitHub Repository
3. ตั้งค่า:
   - **Name:** student-course-api
   - **Environment:** Node
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Plan:** Free

#### **4. ตั้งค่า Environment Variables**
```
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key
PORT=3000
NODE_ENV=production
```

#### **5. Deploy**
- คลิก "Create Web Service"
- รอ deploy สำเร็จ (3-5 นาที)
- ได้ URL: `https://student-course-api.onrender.com`

### 6.2 ข้อดีและข้อเสียของ Cloud Deployment

#### **ข้อดี:**

1. **Accessibility**
   - เข้าถึงได้จากทุกที่ผ่าน internet
   - ไม่ต้องเปิดเครื่องตัวเอง

2. **Scalability**
   - Scale up/down ตามความต้องการ
   - รองรับ traffic สูง

3. **Reliability**
   - Uptime 99.9%
   - Auto-restart ถ้า crash
   - Backup อัตโนมัติ

4. **CI/CD**
   - Auto-deploy เมื่อ push to main
   - Version control integration

5. **Cost-effective**
   - Free tier สำหรับทดสอบ
   - Pay as you use

#### **ข้อเสีย:**

1. **Performance**
   - Free tier มี CPU/Memory จำกัด
   - Cold start (ช้าครั้งแรก)

2. **Cost**
   - ถ้าใช้งานมาก ค่าใช้จ่ายเพิ่ม
   - Paid plans อาจแพง

3. **Dependency**
   - พึ่งพา service provider
   - ถ้า Render down, API down

4. **Control**
   - ควบคุมน้อยกว่า self-hosted
   - ตั้งค่าบางอย่างทำไม่ได้

5. **Security**
   - ข้อมูลอยู่บน cloud
   - ต้องจัดการ secrets ให้ดี

### 6.3 Render Free Tier Limitations

- **Memory:** 512 MB
- **CPU:** Shared
- **Sleep after 15 min inactivity**
- **Cold start:** 30-60 seconds
- **Monthly hours:** Limited

---

## 📊 สรุป

ระบบ Student Course Management System นี้ออกแบบตาม:
- ✅ RESTful API Principles
- ✅ Database Normalization (3NF)
- ✅ MVC Pattern (separation of concerns)
- ✅ Git Workflow (Feature Branching)
- ✅ Cloud Deployment (Render)
- ✅ Environment Variables (Security)

สามารถนำไปใช้จริงได้ และขยายฟีเจอร์เพิ่มเติมได้ เช่น:
- Authentication & Authorization
- Real-time notifications
- File uploads (เช่น รูปโปรไฟล์)
- Advanced reporting
- Email notifications

---

**หมายเหตุ:** อย่าลืมสร้างไฟล์ภาพ ERD.png และ UseCase.png ด้วยเครื่องมือเช่น:
- Draw.io
- Lucidchart
- dbdiagram.io
- PlantUML
